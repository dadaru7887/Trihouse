# P0 시뮬레이션 ACT 설정.
#
# 실제 checkpoint를 아직 받지 못했다. repo/revision/profile은 모두
# UNCONFIGURED이며 이 상태에서는 결정적 fake episode만 실행된다. 실제 OMX
# motion은 절대 나가지 않는다. hardware mode는 세 값을 모두 명시해야만
# 열린다(P1 진입 게이트 3번 항목).
mode: deterministic_fake
repo_id: UNCONFIGURED
revision: UNCONFIGURED
profile: UNCONFIGURED
model_lineage: fake-act/p0-v1
stages:
  - OBSERVE
  - POLICY
  - GRASP
  - VERIFY
  - HANDOVER
